import {NextResponse} from "next/server";
import {z} from "zod";
import {contextSchema,generateText,tutorInstructions} from "@/lib/server/ai";
import {apiFailure,ApiError,readJson,requireAI} from "@/lib/server/guard";
import {resourceContentSchema,resourceKinds,resourceSchema} from "@/lib/workspace";
export const runtime="nodejs";
const schema=z.object({profile:contextSchema,kind:z.enum(resourceKinds),subject:z.string().min(1).max(100),source:z.string().min(1).max(24000),count:z.number().int().min(3).max(20),difficulty:z.enum(["Support","Mixed","Extension"])});
export async function POST(r:Request){try{await requireAI(r);const d=schema.parse(await readJson(r));const prompt=tutorInstructions(d.profile,d.subject)+"\nCreate an original "+d.kind+" at "+d.difficulty+" level. Use "+d.count+" items for cards or quizzes; lesson plans total 50 minutes. Return JSON only: {title,summary,body,cards,questions}. title and summary are strings under 150 and 400 characters. body is Markdown. cards are {id,front,back}. questions are {id,question,options,correctAnswer,explanation}, with four plausible options and zero-based correctAnswer. Include worked explanations. IDs must be unique. Use only cards for flashcards, only questions for quizzes, and only body for other types; unused strings and arrays must be empty. Include misconceptions and application. Lessons need goals, timings, support, extension and an exit ticket. Rubrics must be original formative criteria, not official mark schemes. Do not invent IB assessments or requirements.";const raw=await generateText(prompt,[{role:"user",parts:[{text:d.source}]}],true);let parsed;try{parsed=resourceContentSchema.parse(JSON.parse(raw));}catch{throw new ApiError(502,"The AI draft was not usable. Try a smaller request.");}const checked=resourceSchema.safeParse({...parsed,id:"validation",kind:d.kind,subject:d.subject,program:d.profile.program,level:d.profile.level,examYear:d.profile.examYear,createdAt:new Date().toISOString(),origin:"ai",starred:false,sourceNotes:d.source});if(!checked.success)throw new ApiError(502,"The draft contained incomplete cards or questions. Try again.");return NextResponse.json(parsed,{headers:{"Cache-Control":"no-store"}});}catch(e){return apiFailure(e);}}
